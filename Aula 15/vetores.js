

/* let num = [4, 5, 8, 9, 25]
for(let pos = 0; pos <= num.length; pos++){
    console.log(`á posição ${pos} tem o valor ${num[pos]}`)
}
    */

let num = [4, 8, 5, 10]


for(let pos in num){
    console.log(`á posição ${pos} tem o valor ${num[pos]}`)
}